<%= render 'shared/head_content', title: "New Post" %>

<%= render 'shared/errors', 
    obj: @post,  
    msg: "You cannot create the post because:" %>

<%= render 'post_form', action: "Create Post" %>